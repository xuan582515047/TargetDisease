# 融合大模型与生物关联网络的可解释靶点优选平台实施方案

> 日期：2026-09-16。本文是项目转型方案及执行清单；本次仅编写文档，尚未修改或删除业务代码。
> 执行方式：后续使用 executing-plans 技能按任务推进；每项完成后进行对应验收，不要求并行代理。

**目标：** 在现有项目基础上，实现适合大学生应用开发比赛的轻量级平台：大模型识别种子靶点，本地网络传播算法扩展候选，程序核查证据，通过可交互生物关联网络展示结果，支持比较候选和导出报告。

**架构：** 复用 Next.js、FastAPI、PostgreSQL 和现有账号及 DeepSeek 凭据管理。本机 CPU 承担查询、校验、个性化 PageRank、证据融合和稳定性分析；大模型通过外部 API 调用。采用本地证据与网络快照，后端使用 NetworkX、SciPy，前端使用 Cytoscape.js 渲染关联网络；不引入图数据库、向量数据库、多智能体或独立科学计算服务。

**方案依据：** 已确认保留大模型靶点识别，增加真实的网络传播、稳定性与消融评估，并要求生物关联网络参照用户所给截图实现可视化。本文件同时作为转型设计与实施验收依据；本次只修改方案，不实施代码变更。

## 一、项目定位与边界

建议名称：**靶研助手——融合大模型与生物关联网络的可解释靶点优选平台**。

面向有疾病或机制研究方向、尚未明确研究靶点的学生和课题组。解决“从哪里开始筛选、为什么考虑这个靶点、依据在哪里”的问题。首版不依赖实验室设备、真实患者数据或湿实验结果。

核心流程：

```text
输入研究问题并选择疾病
        ↓
加载该疾病的本地候选池与证据
        ↓
大模型结合问题识别种子靶点、解释推荐理由
        ↓
程序校验种子与引用，执行生物关联网络传播
        ↓
对扩展候选核查证据，融合排序并分析稳定性
        ↓
通过关联网络和候选表查看、筛选、对比结果
        ↓
导出带来源与局限的研究报告
```

### 首版硬边界

- 人类基因/蛋白靶点；暂不处理多物种映射。
- 3 个示例疾病方向，每个约 20～50 个有疾病证据的初始候选；本地计算网络每个疾病最多 2000 个节点、20000 条边。这些是建设规模上限/目标，不代表已有数据。
- 疾病最终名单依据数据完整度选择：至少能提供有效疾病标识、候选基因标识及可追溯关联记录。先完成一个疾病，再扩展另外两个。
- 一次模型识别最多返回 10 个种子靶点，目标为 5～10 个；有效种子不足时如实返回，禁止凑数。网络扩展候选另行计算，不能伪装成模型直接提出的候选。
- 网络传播、交互可视化、种子扰动分析和对照评估均为首版必做项；不训练图神经网络。
- 核心采用一个模型、一次主要识别调用；允许最多一次格式修复调用，不进行循环自我辩论。
- 本机无 GPU 要求，但在线识别依赖外部 API、联网及调用费用；不能宣传为完全离线 CPU 大模型推理。
- 不将候选推荐或数据库关联表述为新靶点发现、因果关系、有效药物或临床结论。

## 二、要做的详细内容

### 2.1 研究问题输入

必填：项目、支持的疾病、研究问题。研究问题限制为 10～1000 个字符；可选填写机制关键词。

疾病采用可搜索下拉框，并显示标准疾病标识，避免让模型随意决定疾病映射。自然语言问题用于表达关注的机制和研究目的。超出疾病覆盖范围时提示当前支持列表，不生成未经核查的分析。

候选基因 CSV 上传列为首版增强项：最多 200 行、1 MB，字段为 `gene_symbol`，可选 `note`。按本地别名映射去重；歧义名称由用户选择，不能静默映射。上传列表与本地数据没有交集时提示原因，不伪造关联。

### 2.2 本地证据数据

优先从 Open Targets 的公开数据整理有限范围快照，不下载和维护全量知识图谱，不要求每次分析实时联网查询数据库。

新增目录 `backend/data/target_evidence/`，包含 `manifest.json`、`diseases.json`、`targets.json`、`evidence.json`。导入脚本先验证，再整体激活新版本；旧报告保留使用过的版本信息和证据副本。

新增 `backend/data/target_network/`，保存 STRING 人类蛋白功能关联子图、来源版本、边证据渠道、原始分值与稳定标识映射。疾病证据与网络快照分别管理版本。STRING 的功能关联不等于直接物理结合，也不能视作疾病因果关系。

蛋白网络按来源蛋白 ID 计算，展示时附标准基因名；首版只接受已明确映射的代表蛋白，歧义映射进入待核查列表，不将同一基因多个蛋白偷偷合并为一个节点。原始边分值按来源定义规范化到 0～1，去自环、去重复；重复无向边使用来源一致条件下的最大分值，不相加。

数据准备阶段围绕初始候选构建固定的最多两跳子图，默认保留关联置信分值 ≥0.7 的边；超过上限时优先保留初始候选，再按边分值及稳定 ID 确定性裁剪。保存裁剪规则和计数。分析与扰动实验均使用同一份固定计算网络，不能每次按模型答案重建不同图后直接比较。

| 数据对象 | 必须保存的信息 |
|---|---|
| 快照清单 | 版本、获取日期、来源链接、许可说明、内容校验值 |
| 疾病 | 内部 ID、标准来源 ID、中文展示名、原始名称、别名 |
| 靶点 | 稳定基因 ID、标准 symbol、名称、别名、可选 UniProt ID |
| 证据 | 唯一 ID、疾病 ID、靶点 ID、数据类型、来源记录 ID/链接、摘要、来源分值（如有）、日期 |

来源分值必须注明其原始含义；不能把关联评分当作成功概率。未提供日期或分值时记为空，不能编造。源数据库聚合记录不能被重复包装成多条独立实验证据。

数据准备本身是首要工作，不得只写接口后用虚构数据宣称平台完成。若来源许可不允许随项目分发，交付获取脚本与导入说明，演示数据按许可要求处理。

### 2.3 大模型候选识别

复用 `backend/app/services/deepseek.py`。新增 `target_identification.py` 负责提示词、结构化输出和候选解析。

输入为研究问题、疾病标识、该疾病最多 50 个候选的简短证据卡片。按字段限制长度，总输入先控制在约 12000 个字符以内，超出时按固定规则裁剪并记录裁剪情况。首版无需向量检索。

模型只能从提供的候选池中选择种子靶点，输出以下结构（`candidates` 在本阶段表示种子列表）：

```json
{
  "question_summary": "对用户研究意图的概括",
  "candidates": [
    {
      "target_id": "候选池中的稳定标识",
      "reason": "与研究问题相关的推荐理由",
      "evidence_ids": ["输入中存在的证据标识"],
      "hypotheses": ["尚待研究验证的机制推测"]
    }
  ],
  "limitations": ["当前证据或数据范围的限制"]
}
```

使用固定版本提示词，低温度参数；记录实际模型名称、调用时间、提示词版本、输入快照版本和原始响应。低温度不保证重新调用得到相同结果，报告复核以保存结果为准。

文献或摘要是待分析数据，不是执行指令；模型不获得运行代码、修改数据或自行调用工具的权限。

### 2.4 证据核查

新增 `evidence_validation.py`，对每个候选执行：

1. 检查返回结构和字段长度。
2. 对模型种子检查是否来自候选池，按稳定标识去重；对网络扩展候选检查是否来自固定网络，并独立匹配疾病证据。
3. 检查引用是否存在，并确实关联当前疾病与靶点。
4. 标记无引用、无效引用、标识不一致和信息缺失。
5. 将模型解释与来源原文/摘要并列展示。

界面使用“引用匹配通过”“部分引用无效”“缺少可核查依据”等精确状态。引用存在并不意味着模型解释被证实；首版不声称完成自动语义真实性判定。

只有数据明确提供反向研究结果时才展示相反证据；无关联记录、低分和空值都不能标记为反对证据。推测单独呈现，不计入证据评分。

### 2.5 排序与对比

结果卡显示：靶点、来源类别（模型种子/网络扩展）、模型理由或网络关联说明、引用状态、疾病关联分值、网络分值、证据覆盖、数据缺口和稳定性。

提供“模型推荐顺序”“疾病证据排序”“融合排序”三种视图；模型推荐视图仅包含种子。模型文字理由不转换为数值置信度。稳定性独立展示，不直接叠加进总分。

融合排序使用两个可解释维度：

- A：来源提供的疾病—靶点关联分值，确认原始范围后规范化至 0～1。
- N：个性化 PageRank 分值除以当前固定计算图内最大分值，范围 0～1；原始分值另存。该规范化分值仅适合本次分析内比较，不跨疾病比较。
- 默认分数 `100 × (0.7 × A + 0.3 × N)`，权重由用户调整且和为 1。该分数只是优选指标；数据库证据与网络数据可能存在来源重叠，不能声称两者是独立验证。
- A 缺失的网络候选放入“探索性候选”，按 N 排序；不填零、不进入完整融合榜。没有可用网络分值的靶点保留疾病证据排序，标记网络覆盖缺失，不捏造 N。
- 证据类型覆盖只作单独展示，不再作为第三个重复加分项。
- 同分按稳定基因 ID 排列，确保相同输入的规则计算一致。

对比页最多选择 4 个靶点，按相同字段并列展示。首版必须实现逐一移除种子的扰动分析；另提供 A 权重为 0、0.1、…、1 的 11 种配置分析。展示排名范围和进入前 5 的次数，种子与网络扩展候选分组报告；这只是算法敏感性分析，不是生物学可靠性验证。

### 2.6 报告与历史记录

复用 Markdown 渲染能力，用固定模板生成报告，不再调用大模型写完整报告。

报告包括：研究问题、疾病与数据范围、种子与扩展候选表、识别理由、证据及来源、推测与缺口、网络类型与版本、传播参数、排序参数、稳定性结果、模型/提示词版本、生成时间及局限。支持同时下载网络 PNG 和带节点/边/参数的 JSON；Markdown 报告记录图文件名与筛选条件。

首版支持 Markdown 报告和 CSV 候选表下载；PDF、Word 和复杂排版列为后续功能。CSV 导出对以 `= + - @` 开头的用户或模型文本作文本转义，防止表格软件执行公式。

旧流程记录只读展示已有结果和报告，并保留旧科学文件下载。旧任务不能从新入口继续执行。旧记录没有相应轻量版字段时展示“旧版记录”，不伪造迁移结果。

### 2.7 页面与接口

| 页面 | 路径及处理 | 功能 |
|---|---|---|
| 工作台 | 保留 `/dashboard` | 项目、最近任务、失败提醒 |
| 研究输入 | 重写 `/target-discovery` | 选择疾病、填写问题、提交 |
| 结果页 | 新增 `/target-discovery/[runId]` | 状态、候选表、筛选、引用核查 |
| 生物关联网络页 | 新增 `/target-discovery/[runId]/network` | 大画布、筛选、节点与边详情、图表联动和导出 |
| 对比页 | 新增 `/target-discovery/[runId]/compare` | 2～4 个候选并列比较 |
| 报告页 | 新增 `/target-discovery/[runId]/report` | 预览及下载 |
| 设置及项目 | 保留现有页面 | 凭据与项目管理 |

后端新增 `api/v1/target_analysis.py`，接口挂在 `/api/v1/target-analysis`：

- `GET /catalog`：支持疾病和快照版本。
- `POST /runs`：输入 `project_id、disease_id、question`，返回 HTTP 202 和记录 ID。
- `GET /runs/{id}`：状态、结果、错误和版本。
- `POST /runs/{id}/rerank`：提交两项权重，只计算本地排序，不再次调用模型。
- `GET /runs/{id}/network`：返回类型化节点、边、计算/显示计数、阈值与截断信息；支持显示阈值、层数和类型筛选，不重跑传播。
- `GET /runs/{id}/stability`：读取已保存的种子扰动和权重敏感性结果。
- `GET /runs/{id}/report?format=md|csv`：下载报告或候选表。

所有记录接口复用所属用户校验。模型调用失败的“重新分析”通过创建新记录实现，并关联原记录；不覆盖原始结果。

### 2.8 任务执行与失败处理

为减少部署服务，首版使用单个后端进程及 FastAPI 后台任务，不运行独立 worker。数据库持久化任务状态；每账号最多一个活动任务，后台模型请求全局并发上限为 2，活动任务总数上限为 10，超出返回繁忙提示。

状态限定为 queued → running → completed / failed。步骤为加载证据与网络、模型识别、种子核查、网络传播、扩展候选核查、稳定性与报告；没有人工审批状态机。

- 后台任务独立建立数据库会话，不复用请求已关闭的会话。
- 每次外部调用超时上限 120 秒；包括格式修复在内最多 2 次请求，单任务执行总时限 300 秒。本地网络及稳定性计算合计预算 30 秒，超过预算明确失败并保留中间结果。HTTP 客户端超时之外还需总时限控制。
- API Key 无效或余额不足直接失败，不自动重试；网络错误提示用户重新发起，不无限循环。
- 模型输出无有效候选时保留空结果和原因，不用预制候选替换。
- 后端重启时将旧 queued/running 标记为中断失败，允许用户新建分析。明确首版不承诺断点续跑，部署限制为单副本、单进程。
- 获取运行资格时使用数据库事务和账号锁；并发限制是明确容量边界，不建设通用队列系统。
- 原始响应和提示词只供所属用户/受控诊断读取，不记录密钥；异常返回经过清理，不向前端转发完整上游响应。
- 扩展现有模型客户端记录 token 用量；接口未返回时标为未知。费用仅在配置了日期明确的单价时估算，不硬编码当前价格。

无网络时允许浏览本地证据及真实历史结果；新模型识别提示不可用。演示历史结果必须注明生成日期和版本，不能显示为实时生成。

### 2.9 大模型引导的网络传播（首版必做）

新增 `network_store.py`、`network_propagation.py`。算法采用带重启的个性化 PageRank：

```text
p(t+1) = α × Wᵀ × p(t) + (1-α) × s
```

W 是由有效关联边权重按行归一化的转移矩阵；s 在通过核查且成功映射到图的种子间均匀分配权重；不把模型自报的置信度作为权重。默认 α=0.85、max_iter=200、tol=1e-8，并记录参数。无向边按双向处理；悬空节点按种子分布回流。

边权含义为来源定义的关联置信分值，不是药理效应强度。疾病节点只用于展示和证据解释，不参与蛋白网络传播，避免疾病星形连边导致人为偏置。

计算结束后保留全图分值，从非种子节点中选取前 20 个作为扩展候选，并独立核查疾病证据。排序考虑网络位置不代表靶点抑制/激活方向，也不能输出“治疗有效”。

无种子映射到图时，网络阶段明确失败，保留已获得的种子证据；部分映射失败时显示未映射数量，仅使用有效部分。只有一个种子时可执行传播，但无法执行留一扰动，稳定性显示“不适用”；不收敛则明确报错，不输出假分值。

解释层展示候选与种子的直接邻居或最多两跳关联及来源。它是辅助理解的局部路径，并不是 PageRank 的完整贡献分解，更不是验证过的生物机制。说明高连接度节点可能排名偏高，并在评估中加入普通网络度数排序基线。

### 2.10 生物关联网络可视化（参照用户图片，首版必做）

**视觉参考：** 用户提供的是“疾病相似性网络”截图。本项目借鉴其蓝色导航、浅蓝背景、白色工作区、左侧设置与图例、右侧大画布、中心聚集与外围放射的布局。图的数据含义改为疾病—候选靶点及蛋白功能关联，不照搬截图中的相似疾病、miRNA 或百分比。

**布局要求：**

```text
蓝色顶部导航：工作台 / 靶点识别 / 生物关联网络 / 历史记录
┌──────────────────────────────────────────────────────────┐
│ 生物关联网络     当前疾病 · 分析编号       缩放/适应/重置/导出 │
├────────────┬─────────────────────────────────────────────┤
│ 网络设置   │ 白色大画布                                  │
│ 阈值滑杆   │ 疾病节点居中，种子在内圈，扩展候选在外圈      │
│ 类型开关   │ 点击节点/边 → 右侧详情抽屉                   │
│ 搜索/层数  │                                             │
│ 图例       │                                             │
│ 网络统计   │                                             │
├────────────┴─────────────────────────────────────────────┤
│ 提示：图中为关联关系；当前筛选只影响展示，不改变原始分析排名 │
└──────────────────────────────────────────────────────────┘
```

桌面 ≥1200px 时左栏约 240～280px，画布占剩余空间且高度不少于 600px。详情默认关闭，以约 320px 抽屉显示；打开时不覆盖关键操作。窄屏将设置折叠，画布高度不少于 420px，保留可替代的节点表。可复用现有导航壳，但必须满足截图的“侧栏设置＋大网络画布”主体结构。

**节点与边的视觉编码：**

| 对象 | 展示样式 | 真实含义 |
|---|---|---|
| 当前疾病 | 大号亮绿色圆形，中心锚点 | 用户选择的疾病，仅展示不参与传播 |
| 模型种子 | 深绿色圆形，内圈，标注“种子” | 经引用/标识核查的模型初始候选 |
| 网络扩展候选 | 金黄色方形，外围 | 网络算法选出的非种子候选 |
| 其余背景蛋白 | 小灰色圆点，默认隐藏 | 参与计算但非当前优选结果的节点 |
| 疾病—靶点证据边 | 浅绿色实线 | 数据源确实存在该疾病—靶点关联 |
| 蛋白功能关联边 | 浅金色实线，透明度随来源分值变化 | 网络快照中存在的关联 |
| 选中节点相邻边 | 加深并适当加粗 | 临时交互高亮，不是新增关系 |

缺少疾病关联证据的扩展候选增加虚线边框/“待核查”徽标，不向疾病节点补画虚构证据边。首版不显示 miRNA、相似疾病和通路节点，除非后续确有独立数据与功能需求。颜色搭配形状、文字和图例，不只靠颜色区分。

节点大小默认按归一化网络分值映射到 14～32px；疾病使用固定约 40px。没有网络分值的节点使用固定小尺寸，并在详情注明。布局距离不代表生物距离，节点面积不代表治疗有效性。

**必备交互：**

1. 拖动节点、平移画布、滚轮缩放、放大/缩小、适应视图、重置布局及全屏。
2. 左侧“关联边显示阈值”滑杆范围为快照最低分值至 1，默认 0.7。明确这是 STRING 来源关联分值，不能标成“疾病相似度”或“药效概率”。低于快照阈值的数据并不存在，界面不能承诺展示。
3. 开关控制种子、扩展候选、背景节点、疾病证据边及蛋白关联边；切换一跳/两跳邻域。孤立节点保留并注明原因。
4. 按 gene symbol 搜索、定位并高亮；点击候选表行定位网络节点，点击节点同步选中表格行。
5. 默认仅显示疾病、种子、Top 10 扩展候选及当前选中节点标签；放大或悬停显示其余标签，避免照搬截图中的文字重叠。
6. 节点详情展示来源类别、稳定 ID、A/N/融合分值、核查状态、稳定性和证据链接，并提供“加入对比”，最多 4 个靶点。
7. 边详情展示关系类型、来源、原始/规范化分值及版本。没有数据字段时显示缺失，不由模型补齐。
8. 网络统计同时显示计算图总数、当前显示数、被筛掉或截断数。导出当前视图 PNG 和含完整计算图及显示参数的 JSON。

**计算与显示分离：** 图算法使用固定快照及计算参数。拖动、缩放、筛选和切换布局只改变视图，不改变排名，也不触发大模型。改变计算阈值或传播参数必须新建分析版本，不能覆盖原报告。本期页面只开放显示筛选和融合权重，传播参数固定由服务端配置。

**技术与规模：** 使用 Cytoscape.js，默认以疾病中心、种子内圈、候选外圈的确定性坐标初始化，提供内置力导向布局切换。采用客户端组件并在页面卸载时销毁图实例。计算图最多 2000 节点/20000 边；默认视图最多 100 节点/500 边，主动展开上限 300 节点/1500 边，超限按关联分值确定性裁剪并提示。不得为了“看起来复杂”复制节点或伪造边。

PNG 导出要带标题、图例、版本和当前阈值，可使用 Cytoscape 画布输出加离屏 Canvas 合成；无须新增服务端截图服务。布局稳定后停止持续动画，开启减少动画偏好时禁用过渡。网络图加载失败时保留候选表和重试入口。

### 2.11 稳定性与消融评估（首版必做）

- 留一扰动：固定图与候选全集，每次移除一个种子并重新均匀分配 s，重新传播，记录完整融合榜的排名范围和 Top 5 出现次数。移除种子不等于删除网络节点。单种子时标记不适用。
- 权重敏感性：固定网络计算结果，评估 A 权重 0～1、步长 0.1 的 11 个配置，不再次调用模型。
- 对照组：仅疾病证据排序；大模型种子＋证据核查；大模型＋网络传播＋证据核查；普通节点度数排序用于检查网络枢纽偏置。
- 记录标识有效率、引用归属匹配率、人工评定的问题相关性、稳定性、耗时和候选覆盖。前后版本使用同一数据版本；分清“种子选择改善”与“扩展候选改善”，不只比较种子命中。
- 每个疾病 3 个问题共 9 例作为演示性初评，不能称为大规模科学验证。若用已知靶点评价检索指标，先隐藏其直接疾病证据和种子身份并冻结评估集；披露来源重叠及大模型预训练知识泄漏风险。
- 没有经过评估，不写“准确率提升”“发现新靶点”。技术贡献表述为已有算法与问题驱动种子、证据核查和交互可视化的集成，不宣称发明 PageRank。

## 三、要保留、改造与删除什么

以下依据当前仓库检查编写。所有“删除”都是后续实施动作，不是本次文档生成动作。

### 3.1 保留并复用

| 文件/模块 | 处理 |
|---|---|
| `backend/app/api/v1/auth.py`、`projects.py`、`credentials.py` | 保留认证、项目和模型凭据管理 |
| `backend/app/core/security.py`、`encryption.py`、`backend/app/deps.py` | 保留密码、凭据加密和用户隔离 |
| `backend/app/services/deepseek.py` | 保留并增加结构化返回、用量记录和错误清理 |
| `backend/app/db/`、`backend/alembic/` | 保留数据库和全部历史迁移 |
| `frontend/app/login/`、`register/`、`projects/`、`settings/api-keys/` | 保留，统一产品名称与文案 |
| `frontend/components/ui/`、`app-shell.tsx`、`auth-shell.tsx` | 保留通用组件 |
| `frontend/components/markdown-report.tsx` | 保留报告展示能力 |
| `frontend/components/molecular-scene.tsx` | 当前被首页和登录布局引用，是展示组件，不按名称误删 |

### 3.2 新流程接通后删除的后端业务代码

下表模块均退出轻量版运行路径。删除前先迁移共享能力、解除导入，并验证后端能启动。

| 路径 | 删除的功能及前置条件 |
|---|---|
| `backend/app/services/lead_identification.py` | 先导化合物需求生成、分子生成和选择 |
| `backend/app/services/cheminformatics.py` | 分子性质、合成难度及化合物过滤 |
| `backend/app/services/docking.py` | 结构下载、口袋推断和对接 |
| `backend/app/services/model_bridge.py` | UAlign、MetaTrans、DecompDiff、DrugCLIP 外部模型桥接 |
| `backend/app/services/scientific_tools.py` | 科学工具命令执行、对接及相互作用分析 |
| `backend/app/services/pipeline_stages.py` | 多阶段科学计算、优化和临床前评估 |
| `backend/app/services/pipeline.py` | 全流程检查点和选靶点/选分子审批；替代执行器上线后删除 |
| `backend/app/services/pipeline_errors.py` | 旧科学计算异常；确认导入清零后删除 |
| `backend/app/services/paper_targets.py` | 当前含结构筛选、配体和口袋流程；不直接作为新证据服务沿用 |
| `backend/app/services/analysis.py` | 旧疾病专家、结构专家、报告专家串联；新识别与模板报告上线后删除 |
| `backend/app/services/workflow.py` | 旧多阶段执行、审批续跑和先导识别；提取必要历史读取逻辑后删除 |
| `backend/app/worker.py` | 独立数据库任务 worker；切换执行方式后删除 |

### 3.3 必须改写，不能整文件删除

- `backend/app/api/v1/runs.py`：保留旧记录列表、详情、受控文件下载；移除旧新建流程、`lead-identification`、`select-targets`、`select-leads`、旧 retry/cancel 和不再使用的 SSE 接口。旧写接口过渡期返回 410 及迁移提示，不能继续入队。先移除对 pipeline/workflow 的顶层导入。
- `backend/app/schemas.py`：移除不再调用的分子生成、选择与审批请求类型；保留历史响应兼容。新类型放到 `target_analysis_schemas.py`，避免扩大单文件。
- `backend/app/db/models.py`：复用 AnalysisRun；新任务 `workflow_version=target-network-v1`，模式固定 auto。输入问题、证据/网络快照版本、种子和算法参数保存到版本化 JSON；结果和稳定性保存到 results_json，网络结果与导出文件保存到 run 所属目录。旧字段、旧状态枚举和旧外键首版不删除，避免历史记录无法反序列化。
- `backend/app/api/v1/__init__.py`：注册新路由，保留历史只读路由。
- `backend/app/main.py`：增加后台任务生命周期与启动中断处理。
- `frontend/lib/api.ts`：增加轻量版接口类型；旧读取保留，旧启动、审批和分子操作方法随调用点一起移除。
- `frontend/components/sidebar.tsx`、`nav.tsx`、`frontend/app/page.tsx`、`dashboard/page.tsx`：删去完整药研流程入口，统一“靶研助手”名称；当前“多肽探索”等不一致文案一并调整。
- `frontend/app/globals.css`：按组件引用清理旧流程专属样式，不删除通用布局与主题。

### 3.4 前端下线与删除清单

| 路径 | 处理 |
|---|---|
| `frontend/app/lead-identification/page.tsx` | 删除旧先导识别页面 |
| `frontend/app/lead-optimization/page.tsx` | 删除优化页面 |
| `frontend/app/preclinical/page.tsx` | 删除临床前评估页面 |
| `frontend/app/pipeline/page.tsx` | 移除完整流程页面；过渡版本可改为到研究入口的跳转 |
| `frontend/components/pipeline-workspace.tsx` | 新结果页和历史只读页完成后删除 |
| `frontend/components/stage-workspace.tsx` | 不再分派新旧多阶段工作台，替换调用后删除 |
| `frontend/components/legacy-target-discovery.tsx` | 历史报告迁入只读组件后删除 |
| `frontend/components/legacy-lead-identification.tsx` | 历史结果迁入只读组件后删除 |
| `frontend/app/target-discovery/page.tsx` | 保留路径，重写为新研究输入页 |

新增 `frontend/components/legacy-run-summary.tsx` 读取旧 JSON、展示报告和下载入口；不重新执行科学计算。旧链接需给出迁移入口或说明，避免无提示失效。

### 3.5 依赖和部署清理

- 从 `backend/requirements.txt` 移除 `rdkit、meeko、vina、gemmi`，前提是剩余代码没有引用。**保留 SciPy，新增 NetworkX**，用于 CPU 稀疏网络传播；不引入 PyTorch、CUDA 或 GNN 训练依赖。
- 从 `frontend/package.json` 移除 `@xyflow/react`，前提是旧图工作台已移除且全库引用为零；同步正常更新锁文件，不手工删除 node_modules 子目录。
- 新增 `cytoscape` 及匹配的 TypeScript 类型支持，用于真实节点—边关联图；旧 React Flow 流程图退出使用，不混为生物网络算法结果。
- 从 `docker-compose.yml` 移除 redis 和 worker 服务；最终只有 postgres、backend、frontend 三个服务。
- 移除旧模型 manifest、对接和科学工具相关 `PHARM_*` 环境变量，按实际使用清理 `.env.example` 和文档；不读取或改写真实密钥值。
- `scientific_artifacts` 卷及后端文件下载能力仍为旧结果服务：保留只读挂载和 `PHARMAGENTS_ARTIFACT_ROOT` 兼容配置，首版不清空该卷。
- 新增 `analysis_artifacts` 持久卷与 `TARGET_ANALYSIS_ARTIFACT_ROOT` 配置，用于新网络结果 JSON 等文件；证据和网络快照只读挂载，新结果按用户/run 校验权限，不与旧科学结果混写。
- 移除不再使用的外部科学模型安装说明、权重准备要求；将旧设计文档标注为历史方案，不删除历史设计记录。
- README 改成轻量版启动、数据准备、API 配置、容量限制和演示步骤，发布运行不使用热重载。

### 3.6 不删除的数据

不删除用户、项目、凭据、旧分析记录、数据库卷、科学结果卷或历史迁移。不执行 `docker compose down -v`。重构前备份数据库并记录恢复方式；先检查旧活动任务并有序结束，再下线 worker，避免新旧执行器同时处理记录。

不为了简化迁移到 SQLite；继续使用已有 PostgreSQL，比改写现有兼容逻辑更省工作。

## 四、实施顺序与交付物

加入算法与交互网络后，按单人开发估算约 17～23 个有效工作日，不是工期保证。数据映射、网络可读性和 API 可用性是主要变数。按下列顺序执行；网络可视化和算法评估不能降为可选包装项。

### 任务 1：制作一个疾病的证据及网络快照（3～4 天）

新增 `backend/data/target_evidence/`、`backend/scripts/import_target_evidence.py`、`backend/app/services/evidence_store.py`、`backend/tests/test_evidence_store.py`。

同时新增 `backend/data/target_network/`、`backend/scripts/import_target_network.py`、`backend/app/services/network_store.py` 和 `backend/tests/test_network_store.py`。

- [ ] 确认数据获取方式、许可及记录字段，选定第一个可完整演示的疾病。
- [ ] 建立稳定 ID 和去重规则，导入至少 20 个候选。
- [ ] 实现按 disease_id 返回候选与证据的查询，返回 manifest 中的版本和校验值。
- [ ] 测试重复 ID、悬空证据引用、错误分值范围及空支持类型列表；无效快照不能激活。
- [ ] 人工抽查至少 10 条记录，核对来源、疾病和基因，记录核查结果。
- [ ] 导入可用 STRING 子图，校验蛋白/基因映射、重复边、自环、边分值和上限裁剪，保存数据版本及许可信息。

交付：一个能够真实查询且来源可追溯的数据集。未完成此项，不进入“真实识别效果”宣称。

### 任务 2：打通模型识别与核查（2～3 天）

新增 `target_identification.py`、`evidence_validation.py`、`target_analysis_schemas.py` 和对应测试；修改 `deepseek.py`。

- [ ] 规定本文件 2.3 的输入输出契约和候选/引用上限。
- [ ] 用固定响应测试正常输出、未知靶点、跨疾病引用、重复候选、无效 JSON、空候选和格式修复失败。
- [ ] 实现候选池构造、一次识别、最多一次修复及引用关联核查。
- [ ] 使用有效 API Key 完成一次真实端到端调用，记录时间与可取得的用量；不以模拟响应替代该验收。
- [ ] 保存模型原始输出与程序核查结果，确保错误不会变成“成功”。

交付：研究问题能够生成带真实引用的候选结果。

### 任务 3：网络传播与稳定性计算（2～3 天）

新增 `backend/app/services/network_propagation.py`、`network_stability.py`、`backend/tests/test_network_propagation.py`、`test_network_stability.py`。

- [ ] 实现 2.9 的固定图传播，保存 s、α、迭代参数、原始分值和归一化分值。
- [ ] 用对称小图验证对称节点同分、分值和约为 1；验证悬空节点、断开分量、空种子、单种子和不收敛处理。
- [ ] 从非种子节点产生前 20 个扩展候选，独立核查疾病证据。
- [ ] 实现留一扰动与 11 组权重分析；测试原图不变、固定候选集、同分规则及单种子不适用。
- [ ] 在真实网络上记录计算规模和耗时，不以测试小图性能替代实际测量。

交付：可复现的网络优选和稳定性结果，不需要 GPU。

### 任务 4：接入任务记录和页面（2～3 天）

新增 `services/target_analysis.py`、`api/v1/target_analysis.py`；修改 main.py、路由注册、frontend/lib/api.ts；新增/改写 2.7 所列输入和结果页。

- [ ] 后台执行器只消费 target-network-v1，保存步骤、状态、输出和错误，串联网络传播及稳定性。
- [ ] 实现单用户并发约束、全局限额、总时限及重启中断处理。
- [ ] 实现输入、进度、成功、空结果和错误页面；使用约 2 秒一次状态轮询，终态停止。
- [ ] 测试账号隔离、重复提交、超时、模型不可用、后端重启以及旧记录访问。
- [ ] 前端能从项目创建任务并查看真实结果。

交付：最小可用闭环。做到这一步后才开始切换默认入口。

### 任务 5：交互生物关联网络（3～4 天）

新增 `frontend/app/target-discovery/[runId]/network/page.tsx`、`frontend/components/target-network/network-canvas.tsx`、`network-controls.tsx`、`network-legend.tsx`、`network-details.tsx`、`frontend/lib/target-network.ts`；后端新增 `services/network_view.py` 提供图展示数据。

- [ ] 实现 2.10 的截图式布局、颜色/形状图例、中心—内圈—外圈布局和力导向切换。
- [ ] 实现拖拽、缩放、重置、全屏、搜索、阈值、类型/邻域筛选及节点/边详情。
- [ ] 实现表格联动、加入对比、统计、截断提示、标签避让和视图/计算分离。
- [ ] 实现 PNG/JSON 导出，PNG 包含图例、标题和筛选状态。
- [ ] 使用真实节点和边验证空网络、孤立节点、高密度图、300 节点上限、窄屏及页面离开后的图实例释放。
- [ ] 对照用户参考图检查主体布局；检查图中每一条边均有对应数据记录，不能为了布局美观补边。

交付：可操作、可解释、可导出的生物关联网络页面，不能以静态图片替代。

### 任务 6：融合排序、对比和报告（2 天）

新增 `services/target_ranking.py`、`services/target_report.py`；新增对比页、报告页及对应测试。

- [ ] 按本文件公式实现评分、空值和同分规则；测试 A=0.8、N=0.5、默认权重时得分为 71。
- [ ] 测试权重负值、权重总和错误、A 缺失和证据类型去重。
- [ ] 实现候选选择、对比、排序参数保存及模板导出。
- [ ] 核对导出引用与界面一致，测试 CSV 文本转义。
- [ ] 将网络版本、种子、算法参数、稳定性及图导出信息加入报告；CSV 候选导入仍是非必需增强项。

交付：用户可以从识别结果形成一份可复核报告。

### 任务 7：切换并清理旧平台（1～2 天）

按第三章逐项处理，新增 `legacy-run-summary.tsx`，保留历史读取和文件授权测试。

- [ ] 备份数据库；停止旧任务新增，检查并结束旧活动任务，停止 worker。
- [ ] 新入口成为默认入口，旧结果切到只读展示。
- [ ] 先改 runs.py 等共享文件，再删除无引用的旧服务与组件。
- [ ] 清理分子计算依赖、redis/worker 服务和旧环境变量；保留 SciPy，加入 NetworkX、Cytoscape.js，同步锁文件和新结果卷配置。
- [ ] 替换旧测试中对已删除功能的要求；保留认证、加密、凭据和历史文件访问安全测试。
- [ ] 完成后端测试、前端构建、三服务启动和一次真实识别回归。

交付：不安装分子对接和外部模型工具也能启动的新平台。

### 任务 8：扩展示例、消融评估与比赛演示（2 天，可与内容整理交错）

- [ ] 按同一数据规范补齐另外两个疾病。
- [ ] 准备每个疾病 3 个真实研究问题，共 9 个固定评估案例。
- [ ] 整理候选与引用的人工核查表，记录标识有效率、引用匹配率、问题相关性和单次耗时。
- [ ] 完成 2.11 的数据库、大模型、网络融合三组对比与度数基线检查，记录差异而非预设效果提升。主观评价写明评价人和标准，没有专家参与就不声称专家验证。
- [ ] 保存一次真实成功结果作为断网时的历史演示，明确标记非实时。
- [ ] 更新 README 和比赛演示脚本。

## 五、验收标准

### 必须通过

- [ ] 无 GPU 的电脑能够启动前端、后端和数据库；无需安装分子计算依赖。
- [ ] 使用真实 API 完成“问题 → 种子 → 网络传播 → 核查 → 交互网络 → 对比 → 报告”。
- [ ] 参考图要求的左侧设置/图例/统计、右侧大画布和蓝白布局完整；种子、扩展候选和疾病在形状与颜色上可区分。
- [ ] 节点拖动、缩放、筛选、搜索、详情、图表联动、PNG/JSON 导出均能使用；显示筛选不改变分析分值。
- [ ] 图中没有虚构疾病关联边，统计与当前显示匹配，超限裁剪明确提示，标签不会在默认视图中大面积重叠。
- [ ] 网络计算参数和数据版本可追溯；完成种子扰动和对照评估，无映射、不收敛和孤立节点有明确处理。
- [ ] 3 个示例疾病的数据真实、有版本和来源；候选规模不足时如实说明。
- [ ] 进入已核查列表的基因标识和引用归属校验通过率为 100%；这不等价于模型论述正确率 100%。
- [ ] 未知或不支持的疾病、无效标识和不存在的引用不会被悄悄接受。
- [ ] 大模型失败可见，不伪装为实时成功；重启后不遗留永久运行状态。
- [ ] 未授权用户不能读取其他用户任务、报告、原始响应或历史文件。
- [ ] 原有账号、项目、历史结果仍可读取，旧药研流程无法再次启动。
- [ ] 后端相关测试通过，前端生产构建通过，README 能指导新环境运行。

### 性能与资源测量

以“4 核 CPU、8 GB 内存”为首次测量参考配置，不作为未经实测的保证。记录操作系统、浏览器、容器资源、图规模和模型名称。简单检索/重排目标为 1 秒内；2000 节点/20000 边上的传播及稳定性总计算预算为 30 秒；默认视图目标在取得数据后 2 秒内可交互。模型等待、浏览器布局和后端算法耗时分别记录，不用缓存结果冒充实时调用速度。超过目标优先缩小快照与显示范围，不以引入 GPU 作为默认解决方式。

## 六、延期功能与范围控制

首版不做本地大模型部署、向量数据库、全网论文爬取、自动判定论文真实性、全疾病覆盖、全量知识图谱、图神经网络训练、多智能体、对接、分子生成、药物优化和临床前预测。有限规模的生物关联网络及其交互可视化属于必做范围，不在延期清单中。

CSV 导入属于核心闭环之后的增强项；PDF/Word 导出、更多疾病、miRNA/相似疾病节点和在线数据更新属于后续版本。网络传播、排名稳定性、关联网络可视化和消融评估均为本轮正式交付。新增功能必须直接改善候选识别、证据核查或报告复核。

## 七、数据与技术核实入口

- Open Targets 平台及定位：https://platform-docs.opentargets.org/
- 数据访问说明：https://platform-docs.opentargets.org/data-access/graphql-api
- 证据说明：https://platform-docs.opentargets.org/evidence
- STRING 关系与分值含义：https://string-db.org/help/scores/
- STRING 数据下载：https://string-db.org/cgi/download
- NetworkX 个性化 PageRank：https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.link_analysis.pagerank_alg.pagerank.html
- Cytoscape.js 布局、交互及导出：https://js.cytoscape.org/

上述链接用于实施时核实字段、数据获取和引用要求；当前文档没有宣称已下载数据或已完成许可审核。模型可用性、收费和 API 能力以实施时服务商实际返回及官方说明为准。

**最终交付边界：大模型识别研究问题相关的种子，本地 CPU 算法从生物关联网络扩展与优选候选，程序核查证据，通过参照用户图片的交互网络解释结果，并形成可复核报告。平台不承诺自动完成药物研发。**
